spring.application.name=servicio-productos

#To indicate which profiles we will be using to read the appropriate configuration. If we need to use several profiles we can use "," coma to
#indicate some of them
#Remember that this info is store in .git properties files through of Server Config that read them
spring.profiles.active=dev

#Pudieramos agrgar el profile si deseamos aplicar una config de testing, dev, prod, etc localhost:8888/default or /testing or /dev ....
spring.cloud.config.uri=http://localhost:8888


